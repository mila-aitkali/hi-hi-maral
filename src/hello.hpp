#ifndef HELLO_HPP
#define HELLO_HPP

// Case-insensitive strcmp-like comparison.
// Returns:
//   < 0 if s1 < s2
//   = 0 if s1 == s2
//   > 0 if s1 > s2
int strcmp_case_insensitive(const char* s1, const char* s2);

#endif
